import React from 'react';
import Navigation from '../components/Navigation';

const Savoir = () => {
    return (
        <div>
            <Navigation/>
            à Savoir
        </div>
    );
};

export default Savoir;